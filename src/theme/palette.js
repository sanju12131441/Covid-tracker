import { colors } from '@material-ui/core';

const black = '#000000';

export default {
  primary: {
    dark: black,
    main: colors.grey[800],
  },
  secondary: {
    dark: black,
    main: black,
  },
}